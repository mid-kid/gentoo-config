# This file is used during python transitions where not all of my packages are
# declared as supported on the new version yet.
# I've tried a few different methods over the years to generate the contents of
# this file, but nothing is ideal. I end up doing it differently every time.

#*/* PYTHON_TARGETS: -* python3_14
#*/* PYTHON_SINGLE_TARGET: -* python3_14

# Check for potentially removable entries:
# rm -f /tmp/use.txt; eix '-#' --installed-with-use python_targets_python3_13 --installed-with-use python_targets_python3_14 | xargs -P4 -I% sh -c "PYTHON_TARGETS=python3_14 emerge -pD --usepkg=n '%' && { echo '%' | tee -a /tmp/use.txt; }"

# emerge -peU --usepkg=n --backtrack=100 --autounmask-backtrack=y @world
# /etc/portage/scripts/check-rebuild
