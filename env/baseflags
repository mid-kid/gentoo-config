# Remove flag customizations
# Default flags for default/linux/amd64/23.0/split-usr/desktop

# profiles/arch/amd64/make.defaults:
CFLAGS="-O2 -pipe"
CXXFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
FCFLAGS="${CFLAGS}"

# profiles/default/linux/amd64/23.0/make.defaults:
LDFLAGS="-Wl,-O1 -Wl,--as-needed -Wl,-z,pack-relative-relocs"

RUSTFLAGS=""
