declare -F post_pkg_preinst > /dev/null || post_pkg_preinst() { env-post_pkg_preinst; }
env-post_pkg_preinst() {
	# Create dirs when lacking tmpfiles
	keepdir /var/lib/ntp
	fowners ntp:ntp /var/lib/ntp
	fperms 0755 /var/lib/ntp
}
